---
interface Props {
    name: string
    slug: string
}

const { name, slug } = Astro.props
---

<a
    href={`/blog/categoria/${slug}`}
    class="text-white inline-block py-1 px-5 bg-coffee-600 text-sm rounded-md mr-3"
>
    {name}
</a>
